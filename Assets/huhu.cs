using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * Game objectives:
 * 1. balance? 
 * 2. don't fall into lava
 * 3. levels w different slopes + difficulties obv
 * 
 * experiment w different lighting techniques: blooming, emmision etc
 */

public class huhu : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
